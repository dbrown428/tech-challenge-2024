import NavBar from "./bar"

/**
 * To make it easier on consumers of this component, I have put the exports
 * in the index file for quick lookup. It also cleans up the import statements
 * in files that use this component.
 * 
 * At this point there's no reason to expose any sub-components (eg. item-action)
 * to consumers, so they are not listed here.
 */
export default NavBar
